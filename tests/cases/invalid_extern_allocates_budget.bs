#[max_memory(1kb)]

@deterministic
@allocates
@stack(32)
extern fn reserve_dma() -> *u8;

fn main() -> i32 {
    let buffer: *u8 = reserve_dma();
    if buffer == (0 as *u8) {
        return 1;
    }
    return 0;
}
