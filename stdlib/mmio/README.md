# BASIS Standard Library - MMIO

Helpers for `volatile` memory-mapped register access.

These wrappers keep low-level register code explicit while staying inside
BASIS type checking and code generation.
